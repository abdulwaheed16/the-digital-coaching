import { LoginForm, RegisterForm } from "@/types/auth";

export const RegisterFormSchemaValidator = (formData: RegisterForm) => {};

export const LoginFormSchemaValidator = (formData: LoginForm) => {};
